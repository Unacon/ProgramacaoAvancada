#include <stdio.h>

int main()
{
    /* p = mat + 1  >> Nesse caso ele irá pegar o endereço de mat da posição 0
      e somar mais um na posição, pegando a posição 1. */
    /* x = (*mat)++ >>Nesse caso ele irá pegar o conteudo que tem mat, sendo a primeira
      posição */
    return 0;

